### Single-Cycle 32-bit MIPS Processor in VHDL

A complete RTL implementation of a 32-bit single-cycle MIPS processor in VHDL,
including datapath, control unit, instruction memory, data memory, and simulation.
